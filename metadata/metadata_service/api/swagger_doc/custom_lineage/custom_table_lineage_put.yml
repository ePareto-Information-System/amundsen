Add Custom Table Lineage
---
tags:
  - "custom_table_lineage"

requestBody:
  content:
    application/json:
      schema:
        type: object
        properties:
          source_id:
            type: string
          dependency_keys:
            type: array
            items:
              type: string
          lineage_type:
            type: string
        required:
          - source_id
          - dependency_keys
          - lineage_type

responses:
  201:
    description: Table Lineage created successfully
    content:
      application/json:
        schema:
          $ref: "#/components/schemas/MessageResponse"
  400:
    description: Invalid request payload

    content:
      application/json:
        schema:
          $ref: "#/components/schemas/ErrorResponse"
